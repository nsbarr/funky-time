# Sample localization file for English. Add more files in this directory for other locales.
# See https://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:
  hello: "Hello world"
  
  activerecord:
      models:
          user:
      attributes:
          user:
              phone: "A phone number that can receive texts"
              prompt: "A prompt, theme, or inspiration"
          
      errors:
          models:
              user:
                  attributes:
                      phone:
                          blank: "is required"
                          invalid: "must be a US # with 10 digits"
                      prompt:
                          blank: "is required"
                          too_long: "is too long"